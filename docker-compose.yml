services:
  drawio2go:
    image: asunfly/drawio2go:v0.1.0
    ports:
      - "13000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
